// Add new data to a project, update name, permissions, whatever; the idea is that once data is set, you can't change it. Will have a confirm button for data submission to make sure people check their data first. (jQuery original button adds the actual submit button? probably the easiest way.)